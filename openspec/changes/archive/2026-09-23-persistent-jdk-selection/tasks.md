# Tasks

## 1. OpenJDK 8 安装可行性门槛

- [x] 1.1 在目标容器条件下核验完整 OpenJDK 8 JDK 的具体发行物、国内可达来源及独立可信 SHA-256；隔离安装后实际验证 `java -version`、`javac -version` 均为 8，记录源、哈希、命令和输出。未通过则停止其余工作包。（版本查询和按需安装；下载源与失败安全）

## 2. 持久化选择与统一命令环境

- [x] 2.1 门槛通过后增加持久 JDK 状态卷和首次默认 21 的初始化；验证已有选择在重建后不被覆盖、空卷默认 21。（默认版本与持久选择）
- [x] 2.2 提供容器内 `jdk list`、`jdk use` 入口和稳定 JDK 环境；验证交互及非交互新命令的 Java、Maven、Gradle 一致，且切回 21 后仍持久。（新命令的统一版本环境；版本查询和按需安装）

## 3. 按需安装与失败安全

- [x] 3.1 使用已验证的 OpenJDK 8 发行物及校验信息支持显式配置可信下载源；验证下载内容的来源、完整性及 `java`、`javac` 的实际版本。（下载源与失败安全）
- [x] 3.2 完成未安装版本的安全下载、安装和切换；验证 `jdk use 8` 后重建不重复下载，下载/校验/中断/不支持版本失败时原选择不变。（版本查询和按需安装；下载源与失败安全）

## 4. 安装升级与交付

- [x] 4.1 更新现有安装、升级与用户操作说明，验证新增卷可预建、旧安装升级与回滚均不删除旧数据，并能按文档执行 `jdk list` 与 `jdk use`。（默认版本与持久选择；版本查询和按需安装）
- [x] 4.2 端到端验证首次 21、切换完整 OpenJDK 8、交互与非交互命令、重建沿用、切回 21 及故障时保持原版本。（所有 requirement）
